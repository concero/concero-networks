{
  "name": "concero-networks",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "bun",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node22 --outdir=dist --format=esm --packages=external",
    "start": "bun run dist/main.js",
    "dev": "bun run src/main.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.13.2",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
